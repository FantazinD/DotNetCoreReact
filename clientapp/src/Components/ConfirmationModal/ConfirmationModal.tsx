import "./ConfirmationModal.css";
import React from "react";

type IProps = {
    onClickConfirm: () => void;
};

const ConfirmationModal = ({ onClickConfirm }: IProps) => {
    return <div>ConfirmationModal</div>;
};

export default ConfirmationModal;
